# Validation of annotations

While some basic validation is done during the extraction process, many others can only be done after all the annotations have already been extracted.

The post-extraction annotation validation mechanism is implemented using the Checker objects $checker.
